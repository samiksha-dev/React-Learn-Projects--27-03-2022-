//Always write it inside the component or function
// component name should be in PascalCase(first letter should be capital)
//we can directly import or we can directly write it using React.hookName eg. const [state,setState]=React.useState
//Dont call Hooks inside loops,conditions or nested functions

//not possible if true(){const [state,setState]=React.useState}
